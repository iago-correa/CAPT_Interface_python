location /static/ {
    alias /var/app/current/staticfiles_collected/;
    expires 7d; # Optional: Add browser caching for static files
    # autoindex on; # TEMPORARY: for debugging only, REMOVE LATER. This would list files if the path is correct but the specific file is missing.
}

# If you also have media files you want to serve locally (from a 'media' folder at the root)
# location /media/ {
#     alias /var/app/current/media/;
#     expires 7d;
# }