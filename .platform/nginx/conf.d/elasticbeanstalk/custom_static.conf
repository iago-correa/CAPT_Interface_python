# .platform/nginx/conf.d/elasticbeanstalk/custom_static.conf
# location /static/ {
#     alias /var/app/current/staticfiles_collected/; # Make sure STATIC_ROOT collects here
#     expires 7d;
#     access_log off;
# }

# location /media/ { # If you also use media files
#     alias /var/app/current/media/; # Make sure MEDIA_ROOT is set to media/
#     expires 7d;
#     access_log off;
# }